DOORDROP_API=http://localhost:3000
OTP_TOKEN=generate-with-openssl-rand-hex-24
POLL_SECONDS=5
CODE_TTL_SEC=300
DELIVERY_ONLY=1

# server side (Vercel project env, not this file)
# OTP_TOKEN=<same shared secret as the firmware>
# EDGE_CONFIG_ID=<your Edge Config store id>
# VERCEL_API_TOKEN=<token with Edge Config write scope>
